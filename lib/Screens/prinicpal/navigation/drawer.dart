import 'package:flutter/material.dart';

class DrawerPrinicpal extends StatefulWidget {
  const DrawerPrinicpal({Key? key}) : super(key: key);

  @override
  _DrawerPrinicpalState createState() => _DrawerPrinicpalState();
}

class _DrawerPrinicpalState extends State<DrawerPrinicpal> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
